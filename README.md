# fbsfilter
Windows (standalone executable or Python script with GUI/console) Purpose: Load a list of leaked credentials (email/username:password), check which accounts are still active (password unchanged) by attempting login to a target service (default: Facebook), with optional proxy support to avoid IP bans. Output working accounts to a file.
